# 同宽对比 · 8031:1892

设计画布：**414 × 3776**（`8031:1892`）

## Phase 5.5 说明

初版交付**未执行**本 Skill 要求的截图对比，导致底栏按钮样式与稿面偏差较大。本节记录底栏修正；全页其余区块待续对比。

对比素材目录：`reference/compare/`

| 文件 | 说明 |
|------|------|
| `figma-bottom-bar.png` | Figma 节点 `8031:1993` 导出 |
| `impl-bottom-bar.png` | 实现页 414px 视口底栏截图 |
| `btn-add-shape.png` / `btn-buy-shape.png` | 按钮矢量导出（scale=2） |

## 底栏 `8031:1993` 差异与修正

| 项目 | 稿面（API） | 初版实现 | 修正后 |
|------|-------------|----------|--------|
| 按钮圆角 | `cornerRadius: 25`（胶囊） | `9px` | `25px` |
| Add to Bag 底 | `#ebf1fe` | `#e8f0ff` | `#ebf1fe` |
| Buy Now 底 | **线性渐变** `#4098ff`→`#356bf1` | 纯色 `#3877f5` | `linear-gradient(145deg, …)` |
| 顶部分割线 | `#e3e3e3` / Line `#eef1f5` | `#e8edf5` | `#e3e3e3` |
| 栏高 | `111px` | flex + 约 10px 内边距 | `grid` 56px + 50px + 底 5px |
| 按钮尺寸 | `187×50`，间距 `8px`，左右 `16px` | `flex:1` + `gap:8` | 同 + `max-width:187px` @414 |
| 价格字重 | SF Pro **510**（Medium） | `600` | `500` |
| total 文案 | `14px` / `#849cbc` / lh22 | 同（曾加 `text-transform`） | 去掉多余 transform |

## 还原度自评（底栏）

修正前约 **55%**（按钮形态、渐变、圆角均不符）。修正后目标 **≥90%**（渐变角度仍可能与稿有 1–2° 偏差，可再按 `btn-buy-shape.png` 微调）。

## 待对比区块（后续）

- [ ] 顶栏 `8031:1988`
- [ ] 标题区 `8031:1900` / `8031:2052`
- [ ] Model Selection 卡片 `8031:1902`
- [ ] Tabs `8031:2014`
