# AUDIT · 8031:1892 详情页-图片详情

**fileKey** `Q1tcc8nYpjqViTXz6kx1ED` · **画布** 414×3776

## 乱稿特征

- 根节点大量 `absolute` 堆叠；MCP `get_design_context` 输出不可直接当最终代码。
- 图层命名误导（`蒙版备份 23` = Model 卡、`位图` = 功能图）。
- 同一规格表在节点树重复两段（清洁/拖地各两份 TEXT），**截图仅见一份** → 实现只保留一组。
- `8031:1923` 位图 `hidden="true"` → 不实现。

## 不实现的系统节点

| nodeId | 名称 | 理由 |
|--------|------|------|
| `8031:2024` / `8031:1987` | Status | 系统状态栏 |
| `8031:1994` | Bottom Bar / Home Indicator | 系统 Home 条 |
| `8031:1996` | 编组 2备份 | 重复状态栏时间条 |

## 节点树 vs 截图

- 帧名「图片详情」实为完整商品详情长页，以 **ref-full.png** 为准。
- 底栏 `8031:1993`：必须用 **API 读渐变/圆角**，禁止猜 9px 圆角或纯色 Buy 按钮。
- 底栏禁止整栏 PNG；文案与双 CTA 必须代码布局（见经验教训 § 组合 CTA）。

## 初版交付问题（已记入 COMPARE）

- 跳过 Phase 5.5，底栏与 Model 卡样式偏差。
- MCP `api/mcp/asset` 当 PNG 会导致图标损坏（本项目已用 Images API）。
